{
  "name": "verifeed-backend",
  "version": "1.0.0",
  "description": "Verifeed API Backend - Claim verification service",
  "main": "dist/server.js",
  "scripts": {
    "build": "echo 'Serverless functions compiled automatically by Vercel'",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "clean": "rm -rf dist",
    "vercel-build": "echo 'No build needed for serverless'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "groq-sdk": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@vercel/node": "^5.8.21",
    "typescript": "^5.4.5"
  }
}
